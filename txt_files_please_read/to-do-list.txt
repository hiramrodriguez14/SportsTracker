Para todos:

Para que el activate_venv les corra tienen que usar el python de windows como interpreter, no el de msys2

Eso lo hacen con los environment variables, anadiendo el path de python a System>environment


-----------------------------------


hubo un problema con el merge porque hiram codio en una rama vieja antes del submission de fase 1, no en la que we submit, pero lo que hice fue mergear a la de alejandro que si estaba actualizada, pero entonces hiram deberia chequear qu eno falte nada de lo suyo

-----------------------------------

con respecto a los merge conflicts:

settings json de hiram y el mio,
    los mezcle en 1

activate venv alejandro y el mio,
    los mezcle en 1

main, app y app 
    los

-----------------------------------

Chequeen el archivo bug_handling > choose_db.py !!!!!


Hay que aplicarlo a todos los DAOs


from bug_handling.config_router import db_config
import psycopg2

class AthleteDAO:
    def __init__(self):
        self.conn = psycopg2.connect(db_config.connection_url)


TO DOOOOOOO

HAY QUE APLICARLO A TODOS LOS DAOS, SO FAR ESTA A:

athletes,
teams.

falta el resto


OTRO TO DOOOOOOO

los controles, solo estan los mios (athlete y teams), falta el resto



-----------------------------------

--
giovanny




-----------------------------------


